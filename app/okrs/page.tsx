import LogoutButton from '../../components/LogoutButton';

const CSS = "<style>\n  :root{\n    --bg:#12151A;\n    --panel:#1B2129;\n    --panel-2:#20272F;\n    --border:#2B333D;\n    --text:#E9E5DA;\n    --muted:#8A93A1;\n    --teal:#4C948C;\n    --teal-soft:rgba(76,148,140,0.16);\n    --coral:#C1594B;\n    --gold:#C9973E;\n    --on:#6FA870;\n    --risk:#DE8A3C;\n    --off:#C1594B;\n  }\n  *{box-sizing:border-box;}\n  html,body{margin:0;padding:0;}\n  body{\n    background:var(--bg);color:var(--text);font-family:'IBM Plex Sans',sans-serif;\n    -webkit-font-smoothing:antialiased;padding:28px 20px 60px;\n  }\n  .wrap{max-width:1040px;margin:0 auto;}\n  header{margin-bottom:24px;}\n  h1{font-family:'Fraunces',serif;font-weight:500;font-size:clamp(26px,5vw,36px);margin:0 0 6px;letter-spacing:-0.01em;}\n  .sub{color:var(--muted);font-size:14.5px;line-height:1.5;max-width:70ch;}\n  h2{font-family:'Fraunces',serif;font-weight:500;font-size:18px;margin:0 0 12px;}\n\n  .cycle-tabs{display:flex;gap:8px;margin:20px 0 22px;flex-wrap:wrap;}\n  .cycle-tab{\n    background:var(--panel);border:1px solid var(--border);color:var(--muted);\n    border-radius:7px;padding:8px 14px;font-size:13.5px;cursor:pointer;font-family:inherit;\n  }\n  .cycle-tab.active{color:var(--text);border-color:var(--teal);background:var(--teal-soft);}\n\n  .overall{\n    background:var(--panel);border:1px solid var(--border);border-radius:10px;\n    padding:16px 18px;margin-bottom:22px;display:flex;align-items:center;gap:16px;flex-wrap:wrap;\n  }\n  .overall .label{font-size:13px;color:var(--muted);min-width:150px;}\n  .overall .score{font-family:'Fraunces',serif;font-size:26px;font-weight:500;}\n  .bar-track{flex:1;min-width:160px;height:10px;background:var(--panel-2);border-radius:5px;overflow:hidden;}\n  .bar-fill{height:100%;background:linear-gradient(90deg,var(--teal),var(--gold));border-radius:5px;transition:width .2s ease;}\n  .overall svg{flex-shrink:0;}\n\n  section{margin-bottom:28px;}\n\n  .objective{\n    background:var(--panel);border:1px solid var(--border);border-radius:10px;\n    padding:16px 18px 18px;margin-bottom:16px;\n  }\n  .obj-head{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:4px;}\n  .obj-title-row{display:flex;gap:8px;align-items:flex-start;flex:1;}\n  .obj-title-row input[type=text]{flex:1;font-weight:500;font-size:15px;}\n  .obj-weight{display:flex;align-items:center;gap:5px;font-size:12.5px;color:var(--muted);white-space:nowrap;}\n  .obj-weight input{width:48px;text-align:right;}\n  .obj-del{background:none;border:none;color:var(--muted);cursor:pointer;font-size:16px;padding:2px 4px;}\n  .obj-del:hover{color:var(--coral);}\n  .obj-progress-row{display:flex;align-items:center;gap:10px;margin:10px 0 14px;}\n  .obj-progress-row .bar-track{height:7px;}\n  .obj-progress-row .pct{font-size:12.5px;color:var(--muted);width:38px;text-align:right;}\n\n  .kr{background:var(--panel-2);border:1px solid var(--border);border-radius:8px;padding:10px 12px;margin-bottom:8px;}\n  .kr-row{display:flex;align-items:center;gap:8px;flex-wrap:wrap;}\n  .kr-row input[type=text]{flex:1;min-width:180px;font-size:13.5px;}\n  .kr-row input[type=range]{width:100px;accent-color:var(--teal);}\n  .kr-row .pct{font-size:12px;color:var(--muted);width:34px;text-align:right;}\n  .kr-weight{display:flex;align-items:center;gap:4px;font-size:12px;color:var(--muted);}\n  .kr-weight input{width:42px;text-align:right;}\n  .kr-del{background:none;border:none;color:var(--muted);cursor:pointer;font-size:13px;}\n  .kr-del:hover{color:var(--coral);}\n\n  .status-dot{\n    width:14px;height:14px;border-radius:50%;border:none;cursor:pointer;flex-shrink:0;\n    padding:0;position:relative;\n  }\n  .status-dot[data-status=\"on\"]{background:var(--on);}\n  .status-dot[data-status=\"risk\"]{background:var(--risk);}\n  .status-dot[data-status=\"off\"]{background:var(--off);}\n\n  .kr-meta{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px;}\n  .kr-meta .mfield{display:flex;flex-direction:column;gap:2px;}\n  .kr-meta label{font-size:10.5px;color:var(--muted);}\n  .kr-meta input[type=text]{font-size:12px;padding:5px 7px;min-width:110px;}\n  .kr-spark{flex-shrink:0;margin-left:auto;align-self:center;}\n\n  .updates{margin-top:8px;}\n  .update-item{\n    display:flex;gap:8px;font-size:12px;color:var(--muted);padding:4px 0;border-top:1px solid var(--border);\n  }\n  .update-item:first-of-type{border-top:none;}\n  .update-item .u-date{white-space:nowrap;color:var(--text);opacity:0.7;}\n  .update-item .u-text{flex:1;}\n  .update-item button{background:none;border:none;color:var(--muted);cursor:pointer;font-size:12px;}\n  .update-item button:hover{color:var(--coral);}\n  .update-add{display:flex;gap:6px;margin-top:6px;}\n  .update-add input{flex:1;font-size:12.5px;}\n  .update-add button{\n    background:none;border:1px solid var(--border);color:var(--muted);\n    border-radius:5px;padding:4px 9px;font-size:12px;cursor:pointer;font-family:inherit;\n  }\n  .update-add button:hover{color:var(--text);border-color:var(--muted);}\n  .updates-toggle{\n    background:none;border:none;color:var(--muted);font-size:12px;cursor:pointer;\n    font-family:inherit;padding:2px 0;text-decoration:underline;text-underline-offset:2px;\n  }\n  .updates-toggle:hover{color:var(--text);}\n\n  .add-kr{\n    background:none;border:1px dashed var(--border);color:var(--muted);\n    border-radius:6px;padding:5px 10px;font-size:12.5px;cursor:pointer;width:100%;\n    font-family:inherit;margin-top:2px;\n  }\n  .add-kr:hover{color:var(--text);border-color:var(--muted);}\n  .add-obj{\n    background:none;border:1px solid var(--border);color:var(--text);\n    border-radius:6px;padding:9px 12px;font-size:13.5px;cursor:pointer;width:100%;\n    font-family:inherit;margin-top:6px;\n  }\n  .add-obj:hover{border-color:var(--teal);}\n  .empty{color:var(--muted);font-size:13.5px;padding:20px 0;text-align:center;}\n\n  input[type=text],input[type=number]{\n    background:var(--bg);border:1px solid var(--border);color:var(--text);\n    border-radius:6px;padding:6px 8px;font-family:inherit;font-size:13px;\n  }\n  input:focus{outline:1px solid var(--teal);}\n  input[type=number]::-webkit-inner-spin-button{opacity:0.6;}\n\n  .eval-panel{\n    background:var(--panel);border:1px solid var(--border);border-radius:10px;\n    padding:16px 18px 20px;\n  }\n  .eval-row{display:flex;align-items:center;gap:10px;margin:10px 0;}\n  .eval-label{width:220px;font-size:12.5px;color:var(--text);flex-shrink:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}\n  .eval-bar-track{flex:1;height:14px;background:var(--panel-2);border-radius:4px;overflow:hidden;position:relative;}\n  .eval-bar-target{position:absolute;top:0;bottom:0;left:0;background:rgba(255,255,255,0.06);border-right:2px dashed var(--muted);}\n  .eval-bar-fill{position:absolute;top:0;bottom:0;left:0;background:linear-gradient(90deg,var(--teal),var(--gold));border-radius:4px;}\n  .eval-pct{width:44px;text-align:right;font-size:12px;color:var(--muted);}\n  .eval-sub{font-size:12px;color:var(--muted);margin-top:-4px;margin-bottom:16px;}\n  .dept-row{display:flex;align-items:center;gap:10px;margin:8px 0;}\n  .dept-label{width:150px;font-size:12.5px;flex-shrink:0;}\n  .dept-bar-track{flex:1;height:10px;background:var(--panel-2);border-radius:4px;overflow:hidden;}\n  .dept-bar-fill{height:100%;background:var(--teal);border-radius:4px;}\n  .dept-meta{font-size:11px;color:var(--muted);width:110px;text-align:right;}\n\n  footer{margin-top:32px;display:flex;justify-content:space-between;align-items:center;gap:12px;flex-wrap:wrap;}\n  .status{font-size:12px;color:var(--muted);}\n  .btn.ghost{\n    background:none;border:1px solid var(--border);color:var(--muted);border-radius:6px;\n    padding:8px 14px;font-size:13.5px;cursor:pointer;font-family:inherit;\n  }\n  .btn.ghost:hover{color:var(--text);border-color:var(--muted);}\n";
const BODY_HTML = "<body>\n<div class=\"wrap\">\n  <header>\n    <h1>OKR-Tracker</h1>\n    <div class=\"sub\">Objectives, Key Results, Gewichtung, Status und Fortschritt \u2014 mit Abteilungs-Zuordnung, Abh\u00e4ngigkeiten, Projekt-Verkn\u00fcpfung und grafischer Auswertung. Zur\u00fcck zum <a href=\"/\" style=\"color:var(--teal)\">Zeitstrahl</a>.</div>\n  </header>\n\n  <div class=\"cycle-tabs\" id=\"cycleTabs\"></div>\n\n  <div class=\"overall\">\n    <div class=\"label\">Gesamtfortschritt (gewichtet)</div>\n    <div class=\"bar-track\"><div class=\"bar-fill\" id=\"overallBar\" style=\"width:0%\"></div></div>\n    <div class=\"score\" id=\"overallScore\">0%</div>\n    <svg id=\"overallSpark\" width=\"120\" height=\"28\" viewBox=\"0 0 120 28\"></svg>\n  </div>\n\n  <div id=\"objectives\"></div>\n\n  <section>\n    <h2>Auswertung</h2>\n    <div class=\"eval-panel\" id=\"evalPanel\"></div>\n  </section>\n\n  <footer>\n    <div class=\"status\" id=\"status\">L\u00e4dt\u2026</div>\n    <button class=\"btn ghost\" id=\"resetBtn\">Alle Eingaben zur\u00fccksetzen</button>\n  </footer>\n</div>\n\n";
const SCRIPT_JS = "const CYCLES = [\n  {id:'c2026h2', label:'H2 2026'},\n  {id:'c2027h1', label:'H1 2027'},\n  {id:'c2027h2', label:'H2 2027'},\n];\n\nconst DEPARTMENTS = ['Marketing','Performance Marketing','CRM / E-Mail','Social Media','Design','Content / Redaktion','Produkt','IT / Tech','IT/KI','Creator','Finance','Legal / Compliance','W&U','Support','Buchhaltung'];\n\nfunction uid(){ return Math.random().toString(36).slice(2,10); }\nfunction todayISO(){\n  const d = new Date();\n  return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;\n}\nfunction isoToDe(iso){\n  const [y,m,d] = iso.split('-');\n  return `${d}.${m}.${y}`;\n}\nfunction todayStr(){ return isoToDe(todayISO()); }\nfunction escapeHtml(s){\n  return String(s).replace(/[&<>\"']/g, c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#39;'}[c]));\n}\n\nfunction kr(text, weight, progress, note){\n  return {\n    id: uid(), text, weight, progress: progress||0,\n    status: 'on', owner: '', department: '', dependsOn: '', project: '',\n    history: progress ? [{date:todayISO(), progress}] : [],\n    updates: note ? [{id:uid(), date:todayStr(), text:note}] : []\n  };\n}\nfunction okr(title, weight, krs){\n  return { id: uid(), title, weight, krs, expandedUpdates: {} };\n}\n\nfunction seedH2_2026(){\n  const o1 = okr('Marketing wird zum wichtigsten Wachstumstreiber f\u00fcr Neukunden. \u2014 Marketing muss einen signifikanten Beitrag zum Unternehmensziel von 20 Mio. \u20ac leisten.', 40, [\n    kr('Marketing generiert \u2265 20 Mio. \u20ac attribuierten Umsatz bis Ende 2026', 18, 0),\n    kr('Performance Marketing steigert den monatlichen Umsatz auf 500.000 \u20ac bis 12/26', 10, 0),\n    kr('ROAS betr\u00e4gt kanal\u00fcbergreifend \u2265 1,4', 7, 0),\n    kr('Mindestens 500.000 \u20ac des Neukundenumsatzes stammen aus nicht bezahlten Kan\u00e4len (SEO, Newsletter, Referral, YT, Social)', 5, 0, 'Aktueller Stand -> Fredrik'),\n  ]);\n  const o2 = okr('Marketing erh\u00f6ht den Customer Lifetime Value bestehender Kunden. \u2014 Marketing endet nicht beim Kauf.', 28, [\n    kr('Monatlicher Bestandskundenumsatz wird um mindestens 15 % gesteigert', 17, 0, 'Aktueller Stand -> Fredrik, Vergleich Dez 26/25'),\n    kr('Winback-Kampagnen erzielen \u226540 % Reaktivierungsquote', 7, 0, 'Aktueller Stand -> Fredrik'),\n    kr('Mindestens 30 % der Kunden kaufen innerhalb von 3 Monaten ein weiteres Produkt', 4, 0),\n  ]);\n  const o3 = okr('HKCM wird zur sichtbarsten Investmentmarke im deutschsprachigen Raum. \u2014 Das zahlt auf langfristiges Wachstum ein.', 14, [\n    kr('600 Mio. organische Social-Impressions wurden bis Ende 2026', 3, 0, 'Owner: Creator'),\n    kr('Organischer Website-Traffic wurde um 40 % gesteigert', 4, 0, 'Aktueller Stand: 37.000 \u00b7 Owner: W&U'),\n    kr('40.000 neue Newsletter-Abonnenten wurden akquiriert', 7, 0, 'Owner: W&U'),\n  ]);\n  o3.krs[0].department = 'Creator'; o3.krs[1].department = 'W&U'; o3.krs[2].department = 'W&U';\n  const o4 = okr('Das Marketing arbeitet datengetrieben, effizient und skalierbar. \u2014 Das ist die interne Optimierung.', 12, [\n    kr('Website-Relaunch erfolgreich abschlie\u00dfen', 3, 0, 'Owner: IT/KI'),\n    kr('2 weitere Revenue Streams wurden erfolgreich am Markt platziert', 4, 0, 'Buch/ HKCM Shop'),\n    kr('Loyalty Programm wurde vollst\u00e4ndig ausgerollt', 4, 0),\n    kr('Die Abstimmung zwischen MKT, IT, KI, Buchhaltung, Support und Prod wurde deutlich verbessert', 1, 0, 'wie wird das gemessen? \u00b7 Owner: alle Abteilungen'),\n  ]);\n  o4.krs[0].department = 'IT/KI';\n  return [o1,o2,o3,o4];\n}\n\nlet state = {\n  activeCycle: 'c2026h2',\n  objectives: { c2026h2: [], c2027h1: [], c2027h2: [] },\n  cycleHistory: { c2026h2: [], c2027h1: [], c2027h2: [] },\n  seeds: []\n};\n\nfunction applySeeds(){\n  if(!state.seeds) state.seeds = [];\n  let changed = false;\n  if(!state.seeds.includes('okr-2026h2-v1')){\n    state.objectives.c2026h2 = seedH2_2026();\n    state.seeds.push('okr-2026h2-v1');\n    changed = true;\n  }\n  return changed;\n}\n\nfunction setStatus(msg){ document.getElementById('status').textContent = msg; }\n\nasync function loadState(){\n  try{\n    const res = await fetch('/api/okr-data');\n    if(res.status === 401){ window.location.href = '/login'; return; }\n    const data = await res.json();\n    if(data && data.value){\n      const parsed = data.value;\n      state.objectives = Object.assign({c2026h2:[],c2027h1:[],c2027h2:[]}, parsed.objectives || {});\n      state.cycleHistory = Object.assign({c2026h2:[],c2027h1:[],c2027h2:[]}, parsed.cycleHistory || {});\n      state.seeds = parsed.seeds || [];\n      state.activeCycle = parsed.activeCycle || 'c2026h2';\n    }\n    if(applySeeds()) saveState();\n    setStatus('Gespeichert \u2014 geteilt mit allen');\n  }catch(e){\n    applySeeds();\n    setStatus('Neu \u2014 noch nichts gespeichert');\n  }\n}\n\nlet saveTimer = null;\nfunction saveState(){\n  recordHistory();\n  setStatus('Speichert\u2026');\n  clearTimeout(saveTimer);\n  saveTimer = setTimeout(async ()=>{\n    try{\n      const res = await fetch('/api/okr-data', {\n        method: 'POST',\n        headers: {'Content-Type':'application/json'},\n        body: JSON.stringify(state)\n      });\n      if(res.status === 401){ window.location.href = '/login'; return; }\n      setStatus(res.ok ? 'Gespeichert \u2014 geteilt mit allen' : 'Speichern fehlgeschlagen');\n    }catch(e){\n      setStatus('Speichern fehlgeschlagen (Verbindung pr\u00fcfen)');\n    }\n  }, 300);\n}\n\nfunction recordHistory(){\n  const cid = state.activeCycle;\n  if(!state.cycleHistory[cid]) state.cycleHistory[cid] = [];\n  const hist = state.cycleHistory[cid];\n  const score = overallScore();\n  const today = todayISO();\n  const last = hist[hist.length-1];\n  if(last && last.date === today){ last.score = score; }\n  else { hist.push({date: today, score}); }\n  if(hist.length > 90) hist.splice(0, hist.length-90);\n}\n\nfunction currentObjectives(){\n  return state.objectives[state.activeCycle] || [];\n}\n\nfunction objProgress(o){\n  if(!o.krs.length) return 0;\n  const totalWeight = o.krs.reduce((s,k)=>s+Number(k.weight||0),0);\n  if(totalWeight <= 0) return 0;\n  const sum = o.krs.reduce((s,k)=>s+Number(k.weight||0)*Number(k.progress||0),0);\n  return sum/totalWeight;\n}\n\nfunction overallScore(){\n  const objs = currentObjectives();\n  const totalWeight = objs.reduce((s,o)=>s+Number(o.weight||0),0);\n  if(totalWeight <= 0) return 0;\n  const sum = objs.reduce((s,o)=>s+Number(o.weight||0)*objProgress(o),0);\n  return sum/totalWeight;\n}\n\nfunction sparklinePath(points, w, h, pad){\n  if(points.length === 0) return null;\n  if(points.length === 1){\n    const y = h - pad - (points[0]/100)*(h-2*pad);\n    return `M ${pad} ${y} L ${w-pad} ${y}`;\n  }\n  const step = (w-2*pad) / (points.length-1);\n  return points.map((p,i)=>{\n    const x = pad + i*step;\n    const y = h - pad - (p/100)*(h-2*pad);\n    return (i===0 ? 'M' : 'L') + ` ${x.toFixed(1)} ${y.toFixed(1)}`;\n  }).join(' ');\n}\n\nfunction renderSparkSvg(points, w, h, color){\n  const pad = 3;\n  if(!points.length){\n    return `<svg width=\"${w}\" height=\"${h}\" viewBox=\"0 0 ${w} ${h}\"><text x=\"${w/2}\" y=\"${h/2+3}\" text-anchor=\"middle\" font-size=\"9\" fill=\"var(--muted)\">\u2013</text></svg>`;\n  }\n  const d = sparklinePath(points, w, h, pad);\n  return `<svg width=\"${w}\" height=\"${h}\" viewBox=\"0 0 ${w} ${h}\"><path d=\"${d}\" fill=\"none\" stroke=\"${color}\" stroke-width=\"1.6\" stroke-linecap=\"round\" stroke-linejoin=\"round\"/></svg>`;\n}\n\nfunction renderTabs(){\n  const wrap = document.getElementById('cycleTabs');\n  wrap.innerHTML = CYCLES.map(c=>\n    `<button class=\"cycle-tab ${c.id===state.activeCycle?'active':''}\" data-cycle-tab=\"${c.id}\">${c.label}</button>`\n  ).join('');\n}\n\nfunction renderOverall(){\n  const score = overallScore();\n  document.getElementById('overallBar').style.width = Math.round(score)+'%';\n  document.getElementById('overallScore').textContent = Math.round(score)+'%';\n  const hist = (state.cycleHistory[state.activeCycle]||[]).map(h=>h.score);\n  document.getElementById('overallSpark').outerHTML = renderSparkSvg(hist, 120, 28, 'var(--gold)').replace('<svg', '<svg id=\"overallSpark\"');\n}\n\nconst STATUS_LABEL = { on:'On Track', risk:'At Risk', off:'Off Track' };\nconst STATUS_ORDER = ['on','risk','off'];\n\nfunction renderObjectives(){\n  const wrap = document.getElementById('objectives');\n  const objs = currentObjectives();\n  if(!objs.length){\n    wrap.innerHTML = '<div class=\"empty\">Noch keine Objectives f\u00fcr diesen Zyklus.</div><button class=\"add-obj\" id=\"addObjBtn\">+ Objective hinzuf\u00fcgen</button>';\n    document.getElementById('addObjBtn').addEventListener('click', addObjective);\n    return;\n  }\n  wrap.innerHTML = objs.map(o=>{\n    const prog = Math.round(objProgress(o));\n    return `<div class=\"objective\" data-obj=\"${o.id}\">\n      <div class=\"obj-head\">\n        <div class=\"obj-title-row\">\n          <input type=\"text\" value=\"${escapeHtml(o.title)}\" placeholder=\"Objective\" data-obj-title=\"${o.id}\">\n        </div>\n        <div class=\"obj-weight\">Gewicht <input type=\"number\" min=\"0\" max=\"100\" value=\"${o.weight}\" data-obj-weight=\"${o.id}\">%</div>\n        <button class=\"obj-del\" data-del-obj=\"${o.id}\" title=\"Objective entfernen\">\u00d7</button>\n      </div>\n      <div class=\"obj-progress-row\">\n        <div class=\"bar-track\"><div class=\"bar-fill\" style=\"width:${prog}%\"></div></div>\n        <div class=\"pct\">${prog}%</div>\n      </div>\n      ${o.krs.map(k=>renderKr(o, k)).join('')}\n      <button class=\"add-kr\" data-add-kr=\"${o.id}\">+ Key Result</button>\n    </div>`;\n  }).join('') + '<button class=\"add-obj\" id=\"addObjBtn\">+ Objective hinzuf\u00fcgen</button>';\n\n  document.getElementById('addObjBtn').addEventListener('click', addObjective);\n}\n\nfunction renderKr(o, k){\n  const updatesOpen = o.expandedUpdates && o.expandedUpdates[k.id];\n  const updatesHtml = (k.updates||[]).slice().reverse().map(u=>`\n    <div class=\"update-item\">\n      <span class=\"u-date\">${escapeHtml(u.date)}</span>\n      <span class=\"u-text\">${escapeHtml(u.text)}</span>\n      <button data-del-update=\"${o.id}|${k.id}|${u.id}\" title=\"Entfernen\">\u00d7</button>\n    </div>`).join('');\n  const history = (k.history||[]).map(h=>h.progress);\n  const status = k.status || 'on';\n  const nextStatus = STATUS_ORDER[(STATUS_ORDER.indexOf(status)+1) % STATUS_ORDER.length];\n  return `<div class=\"kr\" data-kr=\"${k.id}\" data-obj-of-kr=\"${o.id}\">\n    <div class=\"kr-row\">\n      <button class=\"status-dot\" data-status=\"${status}\" data-cycle-status=\"${o.id}|${k.id}|${nextStatus}\" title=\"${STATUS_LABEL[status]} \u2014 klicken zum \u00c4ndern\"></button>\n      <input type=\"text\" value=\"${escapeHtml(k.text)}\" placeholder=\"Key Result\" data-kr-text=\"${o.id}|${k.id}\">\n      <div class=\"kr-weight\">Gewicht <input type=\"number\" min=\"0\" max=\"100\" value=\"${k.weight}\" data-kr-weight=\"${o.id}|${k.id}\">%</div>\n      <input type=\"range\" min=\"0\" max=\"100\" value=\"${k.progress}\" data-kr-progress=\"${o.id}|${k.id}\">\n      <span class=\"pct\">${k.progress}%</span>\n      <button class=\"kr-del\" data-del-kr=\"${o.id}|${k.id}\" title=\"Entfernen\">\u00d7</button>\n      <span class=\"kr-spark\">${renderSparkSvg(history, 70, 22, 'var(--teal)')}</span>\n    </div>\n    <div class=\"kr-meta\">\n      <div class=\"mfield\"><label>Owner</label><input type=\"text\" value=\"${escapeHtml(k.owner||'')}\" placeholder=\"Name\" data-kr-owner=\"${o.id}|${k.id}\"></div>\n      <div class=\"mfield\"><label>Abteilung</label><input type=\"text\" value=\"${escapeHtml(k.department||'')}\" placeholder=\"z. B. CRM\" list=\"deptList\" data-kr-department=\"${o.id}|${k.id}\"></div>\n      <div class=\"mfield\"><label>Abh\u00e4ngig von</label><input type=\"text\" value=\"${escapeHtml(k.dependsOn||'')}\" placeholder=\"z. B. IT, Design\" data-kr-depends=\"${o.id}|${k.id}\"></div>\n      <div class=\"mfield\"><label>Projekt / Kampagne</label><input type=\"text\" value=\"${escapeHtml(k.project||'')}\" placeholder=\"z. B. L1 Trefferquoten-Kampagne\" data-kr-project=\"${o.id}|${k.id}\"></div>\n    </div>\n    <button class=\"updates-toggle\" data-toggle-updates=\"${o.id}|${k.id}\">\n      ${updatesOpen ? 'Status-Updates ausblenden' : `Status-Updates (${(k.updates||[]).length})`}\n    </button>\n    ${updatesOpen ? `<div class=\"updates\">\n      ${updatesHtml}\n      <div class=\"update-add\">\n        <input type=\"text\" placeholder=\"Neues Update\u2026\" data-update-input=\"${o.id}|${k.id}\" maxlength=\"200\">\n        <button data-add-update=\"${o.id}|${k.id}\">Hinzuf\u00fcgen</button>\n      </div>\n    </div>` : ''}\n  </div>`;\n}\n\nfunction renderEvaluation(){\n  const panel = document.getElementById('evalPanel');\n  const objs = currentObjectives();\n  if(!objs.length){ panel.innerHTML = '<div class=\"empty\">Noch keine Daten f\u00fcr diesen Zyklus.</div>'; return; }\n\n  let html = '<div style=\"font-size:12.5px;color:var(--muted);margin-bottom:10px;\">Balken = Zielgewicht am Gesamt-OKR \u00b7 F\u00fcllung = erreichter Anteil</div>';\n  objs.forEach(o=>{\n    const prog = objProgress(o);\n    const achieved = (o.weight * prog / 100).toFixed(1);\n    html += `<div class=\"eval-row\">\n      <div class=\"eval-label\" title=\"${escapeHtml(o.title)}\">${escapeHtml(o.title.split(' \u2014 ')[0])}</div>\n      <div class=\"eval-bar-track\">\n        <div class=\"eval-bar-target\" style=\"width:${o.weight}%\"></div>\n        <div class=\"eval-bar-fill\" style=\"width:${achieved}%\"></div>\n      </div>\n      <div class=\"eval-pct\">${achieved}/${o.weight}%</div>\n    </div>`;\n  });\n\n  // department breakdown\n  const deptMap = {};\n  objs.forEach(o=>o.krs.forEach(k=>{\n    const d = (k.department||'').trim() || 'Nicht zugeordnet';\n    if(!deptMap[d]) deptMap[d] = { weight:0, weighted:0 };\n    deptMap[d].weight += Number(k.weight||0);\n    deptMap[d].weighted += Number(k.weight||0)*Number(k.progress||0);\n  }));\n  const deptEntries = Object.entries(deptMap).filter(([,v])=>v.weight>0).sort((a,b)=>b[1].weight-a[1].weight);\n  const maxWeight = Math.max(...deptEntries.map(([,v])=>v.weight), 1);\n\n  if(deptEntries.length){\n    html += '<h2 style=\"font-size:14px;margin:18px 0 10px;\">Beitrag nach Abteilung</h2>';\n    deptEntries.forEach(([dept,v])=>{\n      const avgProg = v.weight ? Math.round(v.weighted/v.weight) : 0;\n      html += `<div class=\"dept-row\">\n        <div class=\"dept-label\">${escapeHtml(dept)}</div>\n        <div class=\"dept-bar-track\"><div class=\"dept-bar-fill\" style=\"width:${(v.weight/maxWeight*100).toFixed(0)}%\"></div></div>\n        <div class=\"dept-meta\">${v.weight.toFixed(0)}% Gewicht \u00b7 \u00d8 ${avgProg}%</div>\n      </div>`;\n    });\n  }\n\n  panel.innerHTML = html;\n}\n\nfunction addObjective(){\n  currentObjectives().push({id:uid(), title:'', weight:0, krs:[], expandedUpdates:{}});\n  render(); saveState();\n}\n\nfunction findObj(oid){ return currentObjectives().find(o=>o.id===oid); }\nfunction findKr(oid,kid){ const o=findObj(oid); return o ? o.krs.find(k=>k.id===kid) : null; }\n\nfunction addProgressHistory(k, value){\n  if(!k.history) k.history = [];\n  const today = todayISO();\n  const last = k.history[k.history.length-1];\n  if(last && last.date === today){ last.progress = value; }\n  else { k.history.push({date: today, progress: value}); }\n  if(k.history.length > 90) k.history.splice(0, k.history.length-90);\n}\n\nfunction render(){\n  renderTabs();\n  renderOverall();\n  renderObjectives();\n  renderEvaluation();\n}\n\n// ---- events ----\ndocument.getElementById('cycleTabs').addEventListener('click', (e)=>{\n  const btn = e.target.closest('[data-cycle-tab]');\n  if(!btn) return;\n  state.activeCycle = btn.dataset.cycleTab;\n  render(); saveState();\n});\n\ndocument.getElementById('resetBtn').addEventListener('click', ()=>{\n  if(!confirm('Wirklich alle OKRs in diesem Tracker l\u00f6schen?')) return;\n  state.objectives = { c2026h2: [], c2027h1: [], c2027h2: [] };\n  state.cycleHistory = { c2026h2: [], c2027h1: [], c2027h2: [] };\n  render(); saveState();\n});\n\ndocument.addEventListener('click', (e)=>{\n  const delObj = e.target.closest('[data-del-obj]');\n  if(delObj){\n    const oid = delObj.dataset.delObj;\n    state.objectives[state.activeCycle] = currentObjectives().filter(o=>o.id!==oid);\n    render(); saveState(); return;\n  }\n  const addKr = e.target.closest('[data-add-kr]');\n  if(addKr){\n    const o = findObj(addKr.dataset.addKr);\n    if(o) o.krs.push({id:uid(), text:'', weight:0, progress:0, status:'on', owner:'', department:'', dependsOn:'', project:'', history:[], updates:[]});\n    render(); saveState(); return;\n  }\n  const delKr = e.target.closest('[data-del-kr]');\n  if(delKr){\n    const [oid,kid] = delKr.dataset.delKr.split('|');\n    const o = findObj(oid);\n    if(o) o.krs = o.krs.filter(k=>k.id!==kid);\n    render(); saveState(); return;\n  }\n  const statusDot = e.target.closest('[data-cycle-status]');\n  if(statusDot){\n    const [oid,kid,next] = statusDot.dataset.cycleStatus.split('|');\n    const k = findKr(oid,kid);\n    if(k) k.status = next;\n    render(); saveState(); return;\n  }\n  const toggle = e.target.closest('[data-toggle-updates]');\n  if(toggle){\n    const [oid,kid] = toggle.dataset.toggleUpdates.split('|');\n    const o = findObj(oid);\n    if(o){\n      if(!o.expandedUpdates) o.expandedUpdates = {};\n      o.expandedUpdates[kid] = !o.expandedUpdates[kid];\n    }\n    render(); return;\n  }\n  const addUpdate = e.target.closest('[data-add-update]');\n  if(addUpdate){\n    const [oid,kid] = addUpdate.dataset.addUpdate.split('|');\n    const input = document.querySelector(`[data-update-input=\"${oid}|${kid}\"]`);\n    const text = input ? input.value.trim() : '';\n    if(!text) return;\n    const k = findKr(oid,kid);\n    if(k){\n      if(!k.updates) k.updates = [];\n      k.updates.push({id:uid(), date:todayStr(), text});\n    }\n    render(); saveState(); return;\n  }\n  const delUpdate = e.target.closest('[data-del-update]');\n  if(delUpdate){\n    const [oid,kid,uidToDel] = delUpdate.dataset.delUpdate.split('|');\n    const k = findKr(oid,kid);\n    if(k) k.updates = (k.updates||[]).filter(u=>u.id!==uidToDel);\n    render(); saveState(); return;\n  }\n});\n\ndocument.addEventListener('input', (e)=>{\n  if(e.target.dataset.objTitle){\n    const o = findObj(e.target.dataset.objTitle);\n    if(o){ o.title = e.target.value; saveState(); }\n  }\n  if(e.target.dataset.objWeight){\n    const o = findObj(e.target.dataset.objWeight);\n    if(o){ o.weight = Number(e.target.value)||0; renderOverall(); renderEvaluation(); saveState(); }\n  }\n  if(e.target.dataset.krText){\n    const [oid,kid] = e.target.dataset.krText.split('|');\n    const k = findKr(oid,kid);\n    if(k){ k.text = e.target.value; saveState(); }\n  }\n  if(e.target.dataset.krWeight){\n    const [oid,kid] = e.target.dataset.krWeight.split('|');\n    const k = findKr(oid,kid);\n    if(k){ k.weight = Number(e.target.value)||0; renderObjectives(); renderOverall(); renderEvaluation(); saveState(); }\n  }\n  if(e.target.dataset.krOwner){\n    const [oid,kid] = e.target.dataset.krOwner.split('|');\n    const k = findKr(oid,kid);\n    if(k){ k.owner = e.target.value; saveState(); }\n  }\n  if(e.target.dataset.krDepartment){\n    const [oid,kid] = e.target.dataset.krDepartment.split('|');\n    const k = findKr(oid,kid);\n    if(k){ k.department = e.target.value; saveState(); }\n  }\n  if(e.target.dataset.krDepends){\n    const [oid,kid] = e.target.dataset.krDepends.split('|');\n    const k = findKr(oid,kid);\n    if(k){ k.dependsOn = e.target.value; saveState(); }\n  }\n  if(e.target.dataset.krProject){\n    const [oid,kid] = e.target.dataset.krProject.split('|');\n    const k = findKr(oid,kid);\n    if(k){ k.project = e.target.value; saveState(); }\n  }\n  if(e.target.dataset.krProgress){\n    const [oid,kid] = e.target.dataset.krProgress.split('|');\n    const k = findKr(oid,kid);\n    if(k){\n      k.progress = Number(e.target.value);\n      addProgressHistory(k, k.progress);\n      e.target.nextElementSibling.textContent = k.progress+'%';\n      const o = findObj(oid);\n      if(o){\n        const objCard = e.target.closest('.objective');\n        const prog = Math.round(objProgress(o));\n        objCard.querySelector('.obj-progress-row .bar-fill').style.width = prog+'%';\n        objCard.querySelector('.obj-progress-row .pct').textContent = prog+'%';\n      }\n      renderOverall();\n      renderEvaluation();\n      saveState();\n    }\n  }\n});\n\ndocument.addEventListener('keydown', (e)=>{\n  if(e.key === 'Enter' && e.target.matches('[data-update-input]')){\n    e.preventDefault();\n    const [oid,kid] = e.target.dataset.updateInput.split('|');\n    const btn = document.querySelector(`[data-add-update=\"${oid}|${kid}\"]`);\n    if(btn) btn.click();\n  }\n});\n\n(async function init(){\n  const dl = document.createElement('datalist');\n  dl.id = 'deptList';\n  dl.innerHTML = DEPARTMENTS.map(d=>`<option value=\"${escapeHtml(d)}\">`).join('');\n  document.body.appendChild(dl);\n\n  await loadState();\n  render();\n})();\n";

export default function Page() {
  return (
    <>
      <style dangerouslySetInnerHTML={{ __html: CSS }} />
      <div dangerouslySetInnerHTML={{ __html: BODY_HTML }} />
      <div style={{ position: 'fixed', top: 20, right: 20 }}>
        <LogoutButton />
      </div>
      <script dangerouslySetInnerHTML={{ __html: SCRIPT_JS }} />
    </>
  );
}
